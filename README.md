# dotfiles

My dotfiles.


## Bootstrap
1. Install nix
2. Install devbox

```
devbox shell
just
```
Does not change default shell to fish, but installs
~/.zshrc that sets up devbox correctly with zsh (default macos shell).
